name: Post Title
description: Options for displaying title
type: page

extends@:
  - type: archive/entry-title

form:
  fields:

    enabled:
      type: input.checkbox
      label: Display Title
      description: Display post title.
      default: 0

    link:
      unset@: true