name: Comments
description:
type: single

form:
  fields:

    headline.text:
      type: input.text 
      label: Headline 
      default: 'Comments:'

    button:
      type: input.selectize 
      label: Button Class 
      default: button